from main import *
import sys



# method for download the
"""
for j in range(len(lista_peliculas)):

    if lista_peliculas.get(j).id == x:

        br2 = mechanicalsoup.StatefulBrowser()
        answer = br2.open(lista_peliculas.get(j).link)

        if answer.status_code == 200:
            div_magnet = br2.get_current_page().find("div", class_="enlace_descarga")
            magnet_link = div_magnet.contents[3]["href"]
            os.startfile(magnet_link)  # magnet link
"""
